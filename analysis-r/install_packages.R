# Paquetes necesarios para analysis-r/session.Rmd.
# Corré esto una vez antes de abrir el notebook (o instalá los paquetes
# r-cran-* equivalentes vía apt si preferís binarios precompilados).
install.packages(c(
  "readr", "dplyr", "tidyr", "purrr", "jsonlite", "stringr", "ggplot2",
  "broom", "rmarkdown", "knitr"
))
